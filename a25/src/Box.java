public class Box {
    public void box(String d){
        int index = d.indexOf(0);
        int length = d.length();
        for (index = 0; index < length; index++){
            System.out.println(d);
        }

    }

}
